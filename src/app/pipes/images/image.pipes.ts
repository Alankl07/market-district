export function showImageProduct(url: string){
    if(url.length) return url
    return './assets/acém-bovino.jfif'
}

export function showImageMarket(url: string){
    if(url.length) return url
    return './assets/market-default.jpg'
}